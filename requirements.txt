# Hugging Face Space dependencies.
#
# The Space runs the AI service from services/ai-service; this file exists at the
# repository root only because that is where Hugging Face looks.
-r services/ai-service/requirements.txt

# gradio is NOT listed here: Hugging Face pre-installs it and pins the version via
# sdk_version in README.md. Declaring it again risks a conflicting resolve.
