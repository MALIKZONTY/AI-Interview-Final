fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
python-multipart==0.0.20
numpy==2.2.1
# Eye-contact analysis (analyze-video): face mesh + iris landmarks
opencv-python-headless==4.10.0.84
mediapipe>=0.10.14
scikit-learn==1.6.0
# Local STT — pretrained Whisper (CTranslate2); downloads weights on first use
faster-whisper>=1.0.3
# Interviewer voice — local neural TTS; voice model downloads on first use
piper-tts>=1.7.0
# Local semantic similarity for scoring — pretrained MiniLM; pulls torch.
#
# This is the single largest thing in the image: torch and transformers together are
# roughly 500MB on disk and several hundred MB of RAM. It is also entirely optional —
# evaluate.py imports it lazily and falls back cleanly when it is absent, and it only
# feeds the local heuristics that run when the LLM judge is unreachable.
#
# Comment it out to fit a smaller instance; correctness scoring is unaffected while
# Groq is reachable, and the offline fallback simply loses its semantic component.
sentence-transformers>=3.3.0
# Optional heavier STT (PyTorch): pip install openai-whisper
# openai-whisper==20240930
openai>=1.0.0
